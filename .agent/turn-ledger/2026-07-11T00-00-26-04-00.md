# Turn ledger: The Unmapped House

Timestamp: `2026-07-11T00-00-26-04-00`

## Goal

Document the browser-persistence commit boundary so story mutation, StageKit projection, interlude scheduling and durable save state cannot silently diverge when localStorage access, serialization, writes or clears fail.

## Completed

- Compared the complete accessible Publish inventory against the central ledger.
- Excluded `TheCavalryOfRome`.
- Selected only `TheUnmappedHouse` as the oldest eligible direct review.
- Read current root `.agent` state and the previous resume-safe transition audits.
- Read the active story runtime, stage renderer, story descriptors and package scripts.
- Reconstructed the full interaction loop.
- Re-listed all current domains, kits and kit services.
- Traced localStorage load, write and clear behavior.
- Identified mutation-before-durable-commit paths for inspection, Continue and boot.
- Identified interlude scheduling before save confirmation.
- Identified WebGL/listener/RAF acquisition before the boot save can fail.
- Defined persistence result, revision, recovery, cleanup and fixture contracts.
- Changed documentation only.
- Used `main` directly and created no branch or pull request.

## Main result

The current route has no durable commit authority. `saveState()` calls `localStorage.setItem()` directly and returns no result. Story mutation and visual projection occur before the write, while inspection also schedules its delayed interlude before the write. If persistence fails, memory, DOM, timer, StageKit and storage can each represent a different story revision.

## Required implementation boundary

```txt
story-persistence-authority-domain
  -> persistence-capability-admission-kit
  -> versioned-save-envelope-kit
  -> persistence-load-result-kit
  -> persistence-write-result-kit
  -> persistence-clear-result-kit
  -> save-revision-kit
  -> committed-story-snapshot-kit
  -> pending-story-transaction-kit
  -> story-persistence-commit-protocol-kit
  -> projection-from-commit-kit
  -> startup-cleanup-stack-kit
  -> persistence-recovery-kit
  -> persistence-journal-kit
  -> injected-storage-fixture-kit
  -> browser-storage-failure-smoke-kit
```

## Next safe ledge

```txt
TheUnmappedHouse Durable Story Commit Authority
+ Persistence Failure and Recovery Fixture Gate
```

## Validation

```txt
runtime source changed: no
package scripts changed: no
dependencies changed: no
routes changed: no
rendering changed: no
deployment changed: no
branch created: no
pull request created: no
npm run check: not run
browser smoke: not run
persistence failure fixture: unavailable
recovery fixture: unavailable
startup rollback fixture: unavailable
```
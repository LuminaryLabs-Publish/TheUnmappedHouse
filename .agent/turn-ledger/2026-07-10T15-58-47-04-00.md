# Turn ledger: The Unmapped House

Timestamp: `2026-07-10T15-58-47-04-00`

## Goal

Refresh the docs-only architecture breakdown and identify the smallest safe runtime ledge for deterministic story lifecycle transactions, exactly-once browser effects, save reconciliation, and StageKit observations.

## Completed

- Compared the complete accessible `LuminaryLabs-Publish` inventory against central ledger recency.
- Excluded `TheCavalryOfRome` and selected only `TheUnmappedHouse` as the oldest eligible fallback.
- Read the live story runtime, descriptor source, StageKit render host, package scripts, and prior `.agent` state.
- Rebuilt the interaction-loop, domain, service, and kit inventories.
- Added architecture, render, interaction, gameplay, story-authority, save-system, lifecycle, and deploy audits.
- Refreshed all required root `.agent` documents and the kit registry.
- Left runtime source, dependencies, routes, and deployment unchanged.
- Pushed repo-local documentation directly to `main`.
- Updated the central repo ledger and added the timestamped internal change log.

## Main finding

The current route has no explicit lifecycle state or exactly-once effect boundary. Scene completion schedules a timer, interlude and terminal states live only in DOM, saves are shallow-merged without source reconciliation, and StageKit scene replacement has no load/pick/resource-disposal readback.

## Next safe ledge

```txt
TheUnmappedHouse Story Lifecycle Transaction Ledger + StageKit Resource Observation Fixture Gate
```

## Validation record

```txt
runtime source changed: no
package scripts changed: no
branch created: no
pull request created: no
npm run check: not run in connector-only environment
browser smoke: not run
DOM-free lifecycle fixture: unavailable
repo-local documentation pushed to main: yes
central ledger sync: complete
```
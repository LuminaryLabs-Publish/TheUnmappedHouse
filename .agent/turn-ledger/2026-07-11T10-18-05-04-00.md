# Turn ledger: 2026-07-11T10-18-05-04-00

## Repository

`LuminaryLabs-Publish/TheUnmappedHouse`

## Scope

Documentation-only runtime-session lifecycle and Three.js resource-retirement breakdown.

## Completed

- Compared the complete accessible Publish inventory with the central ledger.
- Excluded `TheCavalryOfRome`.
- Selected only `TheUnmappedHouse` because its repo-local transition audit was newer than the central ledger state.
- Read `index.html`, `package.json`, `src/game.js`, `src/story-data.js`, `src/stage-kit.js`, and current `.agent` state.
- Traced startup, interaction, Continue, reset, frame scheduling, listeners, timeouts, scene replacement, rendering, and teardown behavior.
- Cataloged all domains, kits, and services.
- Defined the runtime-session lifecycle parent domain and candidate kit composition.
- Added architecture, render, gameplay, interaction, lifecycle, and deploy audits.
- Refreshed root `.agent` pointers.
- Pushed directly to `main`; no branch or pull request was created.
- Synchronized the central repository ledger and internal change log.

## Principal finding

The runtime has no object that owns the RAF lease, listeners, interlude timeout, scene resources, renderer resources, stage epoch, or teardown result. Scene replacement detaches prior objects without disposal, and page/session teardown has no idempotent stop or dispose contract.

## Runtime change status

```txt
runtime source changed: no
package scripts changed: no
dependencies changed: no
render behavior changed: no
gameplay behavior changed: no
deployment changed: no
```

## Next safe ledge

```txt
TheUnmappedHouse Runtime Session Lifecycle Authority
+ Scene Resource Retirement and Browser Teardown Fixture Gate
```
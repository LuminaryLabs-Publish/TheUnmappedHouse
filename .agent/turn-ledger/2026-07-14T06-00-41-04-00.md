# Turn ledger: terminal completion settlement and resume

**Timestamp:** `2026-07-14T06-00-41-04-00`  
**Repository:** `LuminaryLabs-Publish/TheUnmappedHouse`  
**Branch:** `main`

## Summary

Selected `TheUnmappedHouse` under the oldest eligible documented-selection rule. The run traced the final-scene completion path and found that the visible prototype-complete state is transient DOM text rather than durable story truth.

## Plan ledger

**Goal:** record the selection, evidence, files and validation boundary for this run.

- [x] Enumerate 11 Publish repositories.
- [x] Exclude `TheCavalryOfRome`.
- [x] Compare ten eligible repositories with central tracking.
- [x] Select only `TheUnmappedHouse`.
- [x] Inspect `index.html`, `src/game.js`, `src/story-data.js`, `src/styles.css`, `package.json` and retained `.agent` state.
- [x] Record the interaction loop, domains, 24 kits and services.
- [x] Add architecture, render, gameplay, interaction, terminal-outcome, deploy and central-sync audits.
- [x] Refresh required root `.agent` files.
- [ ] Runtime implementation remains future work.

## Evidence

```txt
final required clue
  -> completion true
  -> delayed interlude
  -> Continue
  -> no next scene
  -> DOM title/text only
  -> no saved terminal outcome

reload
  -> final scene and clues restore
  -> interlude hidden
  -> inspected hotspot re-read returns early
  -> terminal presentation is not reconstructed
```

## Change boundary

Documentation only. No branch or pull request was created. Runtime and deployment behavior were not changed or executed.
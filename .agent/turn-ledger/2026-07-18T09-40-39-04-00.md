# Turn ledger: The Unmapped House render-loop frame allocation

**Timestamp:** `2026-07-18T09-40-39-04-00`  
**Repository:** `LuminaryLabs-Publish/TheUnmappedHouse`  
**Branch:** `main`  
**Status:** `central-reconciled`

## Selection

The full 11-repository `LuminaryLabs-Publish` inventory was compared with the central ledgers. `TheCavalryOfRome` was excluded. All ten eligible repositories were tracked, root-documented and synchronized. `TheUnmappedHouse` was selected as the oldest eligible documented repository and was the only Publish project modified.

## Intent

Document ownership of recurring render-frame callback and camera scratch allocation without changing runtime behavior.

## Source inspected

```txt
README.md
index.html
package.json
src/aspect-frame.js
src/game.js
src/stage-kit.js
src/story-data.js
src/styles.css
.github/workflows/deploy.yml
.agent/START_HERE.md
.agent/current-audit.md
.agent/next-steps.md
.agent/known-gaps.md
.agent/validation.md
.agent/kit-registry.json
central repo ledger and retained change logs
```

## Finding

```txt
requestAnimationFrame(() => this.animate())
  -> creates a source-visible callback closure every animate invocation

this.baseCamera.position.clone()
  -> creates a source-visible THREE.Vector3 every loaded-scene frame
```

At 60 accepted frames per second, the source-visible minimum is 120 transient objects per second. This excludes all unobserved provider, renderer and browser work and is not a measured performance result.

## Output

A timestamped tracker, architecture audit, render audit, gameplay audit, interaction audit, frame-work system audit, deployment audit and central-sync audit were added. Required root `.agent` entry documents and the machine registry were refreshed.

The central repo ledger and matching internal change log were updated in `LuminaryLabs-Dev/LuminaryLabs` on `main`.

## Boundary

Runtime, rendering, story, input, persistence, tests, packages, workflows and deployment were not changed. No branch or pull request was created.
# Turn ledger: Story-save schema and manifest admission

**Timestamp:** `2026-07-13T19-58-19-04-00`  
**Repository:** `LuminaryLabs-Publish/TheUnmappedHouse`  
**Branch:** `main`

## Summary

Selected `TheUnmappedHouse` after comparing the complete Publish inventory with central tracking. All nine eligible repositories were already tracked, root-documented and synchronized, so the oldest eligible timestamp rule applied.

The audit found that successfully parsed localStorage data is trusted without structural or authored-manifest validation. Unknown scene IDs can remain durable while the first scene is shown, and malformed collection fields can crash or alter progression semantics.

## Plan ledger

**Goal:** record one source-backed path from untrusted save bytes to a canonical story state and matching first visible scene.

- [x] Compare ten Publish repositories.
- [x] Exclude `TheCavalryOfRome`.
- [x] Verify nine eligible ledger entries and root `.agent` states.
- [x] Verify documented heads match repository heads.
- [x] Select only `TheUnmappedHouse`.
- [x] Read `src/game.js`, `src/story-data.js`, `src/stage-kit.js`, `src/aspect-frame.js`, package validation and retained persistence documentation.
- [x] Preserve the 24-kit inventory and services.
- [x] Add tracker, architecture, render, gameplay, interaction, save-admission, deploy and central-sync audits.
- [x] Refresh required root `.agent` state.
- [ ] Implement and execute the contract.

## Decision

```txt
current behavior
  JSON parse success -> shallow merge -> live adoption -> immediate rewrite

target behavior
  raw read -> parse -> validate -> manifest check -> migrate/quarantine -> canonical adoption -> visible acknowledgement
```

## Scope boundary

Documentation only. No runtime, story, render, package, workflow or deployment files changed.
# Turn ledger: The Unmapped House render resolution budget

**Timestamp:** `2026-07-17T22-39-01-04-00`  
**Repository:** `LuminaryLabs-Publish/TheUnmappedHouse`  
**Branch:** `main`

## Summary

Selected TheUnmappedHouse by the oldest documented-selection rule after comparing all 11 Publish repositories, excluding TheCavalryOfRome and confirming all ten eligible documented heads were identical to `main`.

## Work completed

- Read the central repo ledger and current repo-local `.agent` state.
- Inspected `package.json`, `src/aspect-frame.js`, `src/game.js`, `src/stage-kit.js` and `src/story-data.js`.
- Identified the full interaction loop, all active domains, all 24 implemented kits and their services.
- Isolated an unbounded DPR, drawing-buffer and multisampled offscreen-target workload.
- Added architecture, render, gameplay, interaction, render-resolution, deployment and central-sync audits.
- Refreshed the required root `.agent` files and kit registry.

## Finding

The renderer and offscreen target are sized from the full aspect-fitted viewport at device pixel ratio up to `2`; the offscreen target uses `samples: 2`; and both scene and post passes execute every RAF. No maximum pixel/sample budget, render scale, quality tier, resize coalescing, allocation fallback or matching-frame digest exists.

## Boundary

Documentation only. No runtime, rendering, story, input, save, package, workflow or deployment behavior changed. No branch or pull request was created.
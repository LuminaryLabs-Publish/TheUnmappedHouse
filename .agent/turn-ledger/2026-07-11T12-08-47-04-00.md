# Turn ledger: The Unmapped House

Timestamp: `2026-07-11T12-08-47-04-00`

## Summary

Selected `LuminaryLabs-Publish/TheUnmappedHouse` under the oldest eligible documented-repository rule. This documentation-only pass maps the missing inspection-command and scene-completion-proof authority.

## Findings

- Side-panel buttons capture full hotspot descriptors.
- Raycast meshes store full hotspot descriptors in `userData`.
- `inspectHotspot()` trusts caller-supplied ids, clue grants, labels, text, and log copy.
- The function uses `currentScene.id` when the callback executes, so a stale descriptor can be rebound to a newer scene.
- Inspections are booleans rather than immutable receipts.
- Clues are global strings without ownership or grant provenance.
- Completion is derived from clue strings rather than current-scene inspection receipts.
- Completion has no proof id, fingerprint, or exactly-once interlude guard.
- Inspection returns no typed result and has no bounded command/result journal.
- Side-panel and raycast parity is untested.

## Required authority

```txt
StoryManifest indexes
  -> id-only InspectionCommand
  -> scene/story/stage admission
  -> canonical hotspot resolution
  -> exactly-once inspection receipt
  -> canonical clue provenance
  -> immutable SceneCompletionProof
  -> persistence-backed commit
  -> UI/debug projection
  -> typed InspectionResult and journal
```

## Validation

```txt
runtime source changed: no
package scripts changed: no
dependencies changed: no
branch created: no
pull request created: no
npm run check: not run; local container could not resolve github.com
browser smoke: not run
new behavioral fixtures: documentation only
```

## Push policy

```txt
repository: LuminaryLabs-Publish/TheUnmappedHouse
branch: main
new branch: none
pull request: none
```

# Turn ledger: 2026-07-14T17-00-55-04-00

**Repository:** `LuminaryLabs-Publish/TheUnmappedHouse`  
**Branch:** `main`  
**Scope:** documentation-only interlude focus and route-admission breakdown

## Summary

Selected TheUnmappedHouse after comparing the complete Publish inventory with central tracking. No eligible repository was new, missing, root-agent-missing or runtime-ahead. TheUnmappedHouse had the oldest synchronized central timestamp.

## Plan ledger

**Goal:** document the hidden-Continue route bypass and define one authority for modal semantics, focus ownership and scene advancement.

- [x] Enumerate 11 accessible Publish repositories.
- [x] Exclude TheCavalryOfRome.
- [x] Compare ten central ledgers and current repository heads.
- [x] Select one repository only.
- [x] Inspect `index.html`, `src/styles.css`, `src/game.js`, `src/story-data.js`, `src/stage-kit.js` and existing `.agent` state.
- [x] Preserve the 24-kit service inventory.
- [x] Add the timestamped audit family.
- [x] Refresh required root `.agent` documents.
- [x] Keep runtime and deployment unchanged.
- [ ] Implement and execute focus/route fixtures later.

## Finding

The interlude is hidden with opacity, pointer-events and `aria-hidden`, but its Continue button remains focusable. `nextScene()` does not validate scene completion. Keyboard activation can therefore advance to the next authored scene before required clues are collected. When the interlude is open, background inspection buttons remain focusable and focus is not moved or restored.

## Result

Required parent authority: `the-unmapped-house-interlude-focus-route-admission-authority-domain`.
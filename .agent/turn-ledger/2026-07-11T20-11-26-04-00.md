# Turn ledger: 2026-07-11T20-11-26-04-00

## Summary

Selected `LuminaryLabs-Publish/TheUnmappedHouse` as the oldest stable eligible Publish repository after excluding active overlapping work in `ZombieOrchard` and `PrehistoricRush`. Documented the stale pointer and click-coordinate authority gap without changing runtime source.

## Plan ledger

**Goal:** make canvas hotspot activation event-local, revision-bound, stale-safe, modality-aware, and semantically equivalent to side-panel activation.

- [x] Compare all ten Publish repositories against central tracking.
- [x] Exclude `TheCavalryOfRome`.
- [x] Confirm nine eligible repositories have root `.agent` state.
- [x] Avoid repositories with active overlapping commits.
- [x] Select only `TheUnmappedHouse`.
- [x] Read `src/game.js`, `src/stage-kit.js`, `src/story-data.js`, `index.html`, and current `.agent` state.
- [x] Identify the interaction loop, domains, 24 implemented kits, and services.
- [x] Add architecture, render, gameplay, interaction, pointer-picking, and deploy audits.
- [x] Refresh root `.agent` entrypoints and kit registry.
- [x] Use `main` only.
- [x] Create no branch or pull request.
- [ ] Implement and run the fixture gate.

## Source finding

```txt
mousemove(event)
  -> updates shared normalized pointer

click(event)
  -> event coordinates discarded
  -> pick() uses shared pointer from prior mousemove
```

The stored pointer begins at `(0, 0)`, has no sample id or revision provenance, and is not guaranteed to describe the click event, active canvas geometry, current camera, current hotspot set, or latest visible frame.

## Current ledge

```txt
TheUnmappedHouse Pointer Observation and Hotspot Pick Authority
+ Event-Local Coordinates / Revision Provenance / Mouse-Touch-Pen Parity Fixture Gate
```

## Validation

```txt
runtime source changed: no
npm run check: not run, github.com DNS unavailable in execution container
browser smoke: not run
branch created: no
pull request created: no
```

# Turn ledger: The Unmapped House

Timestamp: `2026-07-11T17-10-50-04-00`

## Goal

Document the boundary required to keep fixed 16:9 composition independent from bounded, recoverable GPU surface resolution.

## Completed

- Compared the full ten-repository Publish inventory with central tracking.
- Excluded `TheCavalryOfRome`.
- Selected only `TheUnmappedHouse` as the oldest eligible tracked repository.
- Read the current root `.agent` state and source ownership in `src/aspect-frame.js`, `src/game.js`, `src/stage-kit.js`, and `src/story-data.js`.
- Traced initial allocation, DPR sampling, aspect-frame application, resize admission, renderer sizing, post-target sizing, and frame submission.
- Identified all active domains, 24 implemented kits, and their services.
- Added architecture, render, gameplay, interaction, render-surface, and deployment audits.
- Refreshed the root `.agent` pointers and registry.
- Changed no runtime code.
- Used `main` only and created no branch or pull request.

## Main finding

`StageKit.resize()` treats display geometry and internal GPU resolution as the same operation. It can request extremely large multisampled targets on high-DPI displays, repeatedly reallocate them during resize storms, and publish no surface plan, revision, result, fallback, rollback, or first-frame acknowledgement.

## Next safe ledge

```txt
TheUnmappedHouse Render Surface Resolution Authority
+ Pixel Budget / Resize Generation / Fallback / Visible-Frame Fixture Gate
```

# Turn ledger: The Unmapped House

Timestamp: `2026-07-10T14-28-47-04-00`

## Selection

`LuminaryLabs-Publish/TheUnmappedHouse` was selected after comparing the full accessible Publish organization list with the central repo ledgers and sampled root `.agent` state. No eligible repository was new, missing from the ledger, missing root audit state, or otherwise undocumented. `TheUnmappedHouse` had the oldest central update timestamp. `TheCavalryOfRome` remained excluded.

## Work performed

- Re-read `src/game.js`, `src/story-data.js`, `src/stage-kit.js`, `package.json`, and the current root `.agent` documents.
- Reconstructed the browser interaction loop and current authority split.
- Cataloged current domains, next-cut proof domains, kit services, current kits, and next-cut kits.
- Added a command-correlation architecture audit.
- Added StageKit render observation, interaction command, gameplay route, story authority, save-system, and deploy fixture audits.
- Refreshed the root `.agent` pointers and status documents.
- Kept runtime source and package scripts unchanged.
- Synchronized the central repo ledger and internal change log.

## Main finding

The existing prototype has working descriptors, scene progression, persistence, hotspot selection, shaders, and post-processing, but no stable causal ledger. An input cannot be traced through command preflight, result, state transition, DOM projection, save write, interlude/terminal effect, StageKit observation, and diagnostic readback.

## Next safe ledge

```txt
TheUnmappedHouse Story Command Correlation Ledger Refresh + StageKit Observation Fixture Gate
```

## Validation record

```txt
runtime source changed: no
package scripts changed: no
branch created: no
pull request created: no
npm run check: not run
browser smoke: not run
DOM-free story fixture: not run because proof modules do not exist yet
repo-local documentation pushed to main: yes
central ledger sync: complete
```

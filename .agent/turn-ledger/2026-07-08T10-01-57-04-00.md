# Turn Ledger Entry

**Repository:** `LuminaryLabs-Publish/TheUnmappedHouse`

**Timestamp:** `2026-07-08T10-01-57-04-00`

**Branch target:** `main`

## Goal

Break down and update internal docs for one eligible `LuminaryLabs-Publish` repository after comparing the full accessible Publish repo list against central `LuminaryLabs-Dev/LuminaryLabs` tracking.

## Selected repo

```txt
LuminaryLabs-Publish/TheUnmappedHouse
```

## Selection reason

No checked non-Cavalry Publish repo was fully new, absent from the central ledger, missing root `.agent/START_HERE.md`, or recently added but undocumented.

`TheUnmappedHouse` was selected as the oldest observed eligible fallback because it has an unresolved story-authority source seam and its last sampled alignment was older than the active checked set.

## Work completed

- Refreshed root `.agent/START_HERE.md`.
- Refreshed `.agent/current-audit.md`.
- Refreshed `.agent/known-gaps.md`.
- Refreshed `.agent/next-steps.md`.
- Refreshed `.agent/validation.md`.
- Added timestamped tracker entry.
- Added timestamped architecture DSK breakdown.
- Added timestamped render result-readback audit.
- Added timestamped interaction/source wire map.
- Refreshed `.agent/kit-registry.json`.
- Updated central `LuminaryLabs-Dev/LuminaryLabs` repo ledger.
- Added central internal change-log entry.

## Main finding

`src/game.js` is now the right source cutover seam.

The renderer and story can stay visually stable while story authority becomes a small DOM-free reducer surface consumed by the existing UI.

The next source implementation should add command envelopes, result records, reason codes, snapshots, projection helpers, and fixture replay before adding new rooms, audio, inventory, or StageKit renderer extraction.

## Current next safe ledge

```txt
TheUnmappedHouse Story Authority Source Wire Map
```

## Validation performed

```txt
GitHub connector repo-list read
GitHub connector central ledger/search read
GitHub connector repo-local file reads
GitHub connector writes to main
```

## Validation not performed

```txt
local checkout
npm install
npm run check
python static server
browser smoke
Playwright smoke
GitHub Pages live route check
runtime source edit
```

# Turn ledger: The Unmapped House pointer presence retirement

**Timestamp:** `2026-07-17T05-03-18-04-00`  
**Repository:** `LuminaryLabs-Publish/TheUnmappedHouse`  
**Branch:** `main`

## Completed

- Compared all 11 `LuminaryLabs-Publish` repositories against the ten eligible central ledgers.
- Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- Confirmed no new, ledger-missing, root-agent-missing, undocumented or runtime-ahead eligible repository.
- Selected only TheUnmappedHouse by the oldest synchronized timestamp.
- Reviewed `index.html`, `src/game.js`, `src/stage-kit.js`, `src/story-data.js`, `package.json` and retained audit state.
- Identified the complete interaction loop, active domains, all 24 implemented kits and their services.
- Isolated stale hover-label and camera-parallax state after pointer exit, cancellation, focus loss, visibility change or scene replacement.
- Defined one parent pointer-presence authority and 18 coordinating surfaces.
- Added architecture, render, gameplay, interaction, pointer-presence, deployment and central-sync audits.
- Refreshed the root `.agent` routing and validation records.
- Changed documentation only.

## Finding

`StageKit` updates `hovered`, the hover label and camera-parallax caches only from canvas `mousemove`. It has no pointer-exit or scene-replacement retirement path. The last accepted hover and parallax sample can remain visible after its evidence is no longer valid.

## Required authority

`the-unmapped-house-pointer-presence-hover-parallax-retirement-authority-domain`

## Not completed

Runtime listeners, pointer state, hover projection, parallax behavior, tests, artifact proof and Pages behavior were not changed or executed.
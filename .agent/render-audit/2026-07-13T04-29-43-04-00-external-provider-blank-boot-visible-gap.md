# Render audit: external-provider blank-boot visible gap

**Timestamp:** `2026-07-13T04-29-43-04-00`

## Summary

The first meaningful rendered frame depends on a remote Three.js module imported before application code executes. A rejected module graph has no provider-independent visual failure path, so the shell can remain in its static `Loading` state without stating whether the provider was unavailable, rejected or incompatible.

## Plan ledger

**Goal:** require each visible stage frame or visible boot failure to cite one terminal renderer-provider result.

- [x] Trace static HTML through module evaluation and StageKit construction.
- [x] Identify the provider-independent visible surface.
- [x] Identify absent provider/frame provenance.
- [x] Define visible acceptance and failure receipts.
- [ ] Add executable blocked-provider and Pages fixtures.

## Current visible path

```txt
index.html
  -> paints fixed shell
  -> scene title starts as "Loading"
  -> loads src/game.js

src/game.js
  -> waits for complete static dependency graph

src/stage-kit.js
  -> imports Three.js from unpkg
  -> creates renderer only after provider evaluation
```

## Gap

```txt
provider result ID: absent
provider generation: absent
provider fingerprint: absent
renderer boot phase: absent
stage construction result: absent
static failure copy: absent
retry/recovery action: absent
first provider-backed frame ack: absent
first visible provider-failure frame ack: absent
```

A provider failure occurs before the runtime can change `#scene-title`, `#scene-text`, `#state-debug` or the stage canvas. Console output from the browser module loader is not a game-owned visible result.

## Required visible states

```txt
ResolvingProvider
ProviderAccepted
ConstructingStage
StageReady
ProviderFallbackAccepted
ProviderUnavailable
ProviderIntegrityRejected
ProviderContractRejected
ProviderTimedOut
```

The first visible `StageReady` frame must cite:

```txt
runtimeSessionId
providerPolicyRevision
providerId
providerGeneration
providerVersion
providerFingerprint
stageGeneration
renderFrameSequence
presentedAt
```

A non-accepted frame must be generated by provider-independent HTML/CSS/JavaScript and expose a bounded retry or recovery action.

## Validation boundary

No renderer, provider source, HTML, CSS or workflow was changed. No visible failure or recovery path was executed.